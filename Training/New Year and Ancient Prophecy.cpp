#include<bits/stdc++.h>
#define pb push_back
#define pii pair<int,int>
#define pll pair<long long, long long>
#define mp make_pair
#define I
using namespace std;

typedef long long ll;
const int MAX = 5006;
char num[MAX];
ll mod = 1000000007ll;

ll memo[MAX][MAX];

ll solve(int cur, int prev){



}

int main(){
  //ios_base::sync_with_stdio(false);
  //cin.tie(0);
  memset(memo, -1, sizeof(memo));
  int n;
  scanf("%s", &num);
  printf("%I64d")
  return 0;
}
